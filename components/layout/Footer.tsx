export function Footer() {
  return (
    <footer className="mt-auto border-t border-border py-6 text-center text-xs text-muted-foreground">
      <p>Plantry — every recommendation shows its reason, not just a score.</p>
    </footer>
  );
}
